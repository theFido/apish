enum Mood { happy, mad, sad }

enum Options { first, second }

struct Person {
 name string required `example: 123` `go: Name` "hello, world"
 age int
 mood Mood
 favoriteFood []string
}

struct ClassRoom {
    students []Person
}